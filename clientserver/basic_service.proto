syntax = "proto3";
package clientserver;

service Messenger {
  rpc SendMessages (stream Command) returns (Response) {}
}

message Command {
  string commandText = 1;
}

message Response {
  string response = 1;
}

