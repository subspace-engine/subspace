syntax = "proto3";
package clientserver;

service Game {
  rpc Play(stream Command) returns (stream Info) {}
}

message Command {
  string text = 1;
}

message Info {
  string text = 1;
}